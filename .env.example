# ═══════════════════════════════════════════════════════════════════════════════════════
# FUNTOA SMIE - Configuration Environment Variables
# ═══════════════════════════════════════════════════════════════════════════════════════
# 
# IMPORTANT: Duplicate this file to .env and fill in the actual values
# Ne jamais commiter .env dans Git (il est dans .gitignore)
#
# Pour LOCAL (dev):         cp .env.example .env
# Pour PRODUCTION (VPS):    Utiliser docker-compose.prod.yml avec override
#
# ═══════════════════════════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────────────────────────────
# 📊 BASE DE DONNÉES (MariaDB/MySQL)
# ─────────────────────────────────────────────────────────────────────────────────────────
DB_HOST=mysql
DB_PORT=3306
DB_NAME=db_funtoasmie
DB_USER=root
DB_PASSWORD=
DB_ROOT_PASSWORD=

# ─────────────────────────────────────────────────────────────────────────────────────────
# 🔐 BACKEND - SÉCURITÉ & AUTHENTIFICATION
# ─────────────────────────────────────────────────────────────────────────────────────────
NODE_ENV=production
PORT=5000

# JWT - Secrets pour les tokens d'authentification
# ⚠️ GÉNÉRATION: openssl rand -hex 32
JWT_SECRET=952484620a2e86d77f88a86030bf96ebf91a4ae445e70a21e600a4cfba0bb1a1
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# Bcrypt - Nombre de rounds pour le hash des mots de passe
BCRYPT_ROUNDS=12

# CORS - Origines autorisées pour les requêtes cross-origin
# Format: URL1,URL2,URL3 (séparées par virgules, sans espaces)
CORS_ORIGIN=http://localhost,http://localhost:3000,http://37.59.118.164,https://app.funtoa-smie.com

# ─────────────────────────────────────────────────────────────────────────────────────────
# 🎨 FRONTEND - CONFIGURATION API
# ─────────────────────────────────────────────────────────────────────────────────────────
# URL de base pour l'API backend depuis le frontend
# LOCAL: http://localhost:5000/api
# PROD:  http://37.59.118.164/api (via nginx reverse proxy)
REACT_APP_API_URL=http://localhost:5000/api

# ─────────────────────────────────────────────────────────────────────────────────────────
# 🐳 DOCKER - PORT MAPPING
# ─────────────────────────────────────────────────────────────────────────────────────────
BACKEND_PORT=5000
MYSQL_PORT=3306
FRONTEND_PORT=3000

# ─────────────────────────────────────────────────────────────────────────────────────────
# 📋 NOTES POUR LA PRODUCTION
# ─────────────────────────────────────────────────────────────────────────────────────────
# 
# 1. JWT_SECRET:
#    - Générer une clé sécurisée: openssl rand -hex 32
#    - Ne JAMAIS utiliser la valeur par défaut en production
#
# 2. DB_PASSWORD & DB_ROOT_PASSWORD:
#    - Utiliser des mots de passe forts et uniques
#    - Générer: openssl rand -base64 32
#
# 3. CORS_ORIGIN:
#    - En production: ajouter uniquement les domaines approuvés
#    - Format: https://app.example.com,https://www.example.com
#
# 4. REACT_APP_API_URL:
#    - LOCAL:      http://localhost:5000/api
#    - PRODUCTION: http://37.59.118.164/api  OU  https://api.funtoa-smie.com/api
#
# 5. SSL/TLS:
#    - À implémenter dans nginx avec Let's Encrypt (certbot)
#    - Voir docs/DEPLOY.md pour les instructions
#
